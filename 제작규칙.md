# 인스타그램 카드뉴스 제작 규칙

이 문서는 카드뉴스를 만들면서 사용자가 직접 지시한 규칙들을 한 곳에 모아둔 것입니다.
새로운 카드를 만들 때 반드시 이 규칙들을 따라야 합니다.

---

## 1. 디자인 규칙

### 1-1. 여백
- **하단 여백을 너무 크게 잡지 말 것**
- 카드 하단에 빈 공간이 과하게 남지 않도록 패딩 적정 유지
- 현재 적용 값:
  - `.summary` `padding: 90px 90px 130px`
  - `.point` `padding: 100px 90px 130px`
  - `.cta` `padding: 130px 80px 130px`
  - `.brand-mark { bottom: 60px }`
  - `.point__date / .point__page { top: 55px }`

### 1-2. 글자 크기 (본문 확실히 크게)
- 인스타 1080×1350 캔버스에서 멀리서도 잘 읽히도록 키울 것
- 사용자가 두 차례 "더 키워달라"고 요청한 결과 적용된 값:
  - 포인트 본문(`.point__main`): **44px**
  - 포인트 부설명(`.point__sub`): **32px**
  - 통계 라벨(`.stat__label`): 34px
  - 통계 값(`.stat__value .num`): 62px
  - 통계 델타(`.stat__value .delta`): 34px
  - 섹션 타이틀(`.summary__title`, `.point__title`): 56px
  - 마무리 한 줄(`.closing__text`): 56px
  - 커버 부텍스트(`.cover__sub`): 36px
  - 라벨/뱃지/페이지 번호: 22~26px

### 1-3. 색상 / 테마
- 다크 카드: 갈색~크림 그라디언트 + 오렌지 포인트 (cards 1, 7, 8)
- 브라이트 카드: 크림 배경 + 오렌지 강조 (cards 2~6)
- 강조 효과: `.hl` 형광펜 스타일 + `.hl-box` 박스 스타일

---

## 2. 콘텐츠 규칙

### 2-1. 이모티콘 / 특수기호 사용 금지
다음과 같은 장식성 기호는 **절대** 사용하지 않는다.
- 별표/장식: `✦` `★`
- 화살표: `▲` `▼` `→`
- 원숫자: `①` `②` `③`
- 곱셈/근사 기호: `×` `≈`
- 큰 따옴표 장식: `"` (closing card의 quote-mark)

**예외(허용):** 인용 표시용 일반 따옴표 `" "` 는 본문 내 인용 시 사용 가능 (예: `"BTC 저평가" 응답`)

### 2-2. 영어 UI 라벨 사용 금지
- "DAILY BRIEFING", "BTC MARKET REPORT", "REAL TRADER'S VIEW", "EE-JIN-TRADER", "POINT 1 / 4" 같은 **영문 UI 라벨은 모두 한국어로 바꾸거나 제거**
- 한국어 변환 예시:
  - `BTC MARKET REPORT` → `비트코인 시황 리포트`
  - `POINT 1 / 4` → `포인트 1 / 4`
  - `NFP` → `고용지표`
- **예외(허용):** BTC, ETF, EMA, $, IBIT, CFTC, SEC 등 **금융/암호화폐 업계 표준 용어는 그대로 사용 가능**

### 2-3. "매일 새벽 시황 받기" 류 광고성 멘트 금지 (본문)
- "매일 시황 받기", "DAILY BRIEFING", "구독", "매일 받아보세요" 같은 광고성·유도성 안내 멘트는 본문 카드(1~7)에 넣지 않는다
- **마지막 CTA 카드는 예외:** 팔로우 CTA는 마지막 한 장에서만 허용 (자세한 내용은 §3)

### 2-4. 리딩성 문체 금지 (중요)
매수/매도를 권하거나 방향을 단정 짓는 문장은 **모두** 금지.

- 금지 표현 예시:
  - "다시 시작이다", "지지선이 되는 순간", "다시 몰린다"
  - "신호다", "전환이다", "관문", "필수 시그널"
  - "타깃 $XXX", "시야 확보", "연료가 쌓이는 중", "단기 모멘텀"
  - "트레이더 시각", "주의해야 한다"
- 권장 표현:
  - "보고됐다", "집계됐다", "기록됐다" (사실 보고)
  - "자주 언급되는 구간", "일부 분석에서는 ~로 본다" (인용·관찰)
  - "참고 지표", "함께 살펴보는 데이터" (중립 표현)
  - 시점·출처 명시 (`시점 기준`, `OOO 조사 결과`, `통과 여부 미정`)

### 2-5. 줄바꿈 원칙 (중요)
- 텍스트가 자동 줄바꿈으로 **의미 단위가 어색하게 끊기지 않도록** 명시적 `<br/>` 사용
- 금지 패턴 예시:
  - 단어 한복판에서 끊김: `"BTC 저평가" 응 / 답`
  - 핵심구가 따라붙는 어색한 분절: `시장에서 자주 언급되는 / 구간`
  - 한두 글자만 다음 줄에 남는 경우: `~로 본 / 다`
- 권장 패턴:
  - 어절(주어/목적어/술어) 단위로 자연스럽게 끊기
  - 위·아래 줄 길이가 비슷하도록 균형 맞추기
  - 강조(`.hl`)는 가능한 한 한 줄에 모이도록 배치
- 작성 시 카드를 한 번 렌더해보고 어색한 줄바꿈이 있으면 `<br/>` 추가

### 2-6. 면책 문구 (필수)
- **카드 7 하단 영역에 포함** (CTA 카드와 통합):
  - "본 콘텐츠는 학습·정보 공유 목적입니다. 투자 권유가 아니며,"
  - "모든 투자 결정과 결과의 책임은 본인에게 있습니다."
- 블로그 텍스트 파일에도 마지막에 동일한 안내 문단 추가
- 디자인: **28px** (브랜드 로고와 동일 크기) / 연한 크림(`rgba(245, 233, 216, 0.45)`) — 가독성 확보, 메인보다 약하게

---

## 3. 카드 구조 (총 7장)

| 카드 | 역할 | 테마 |
|------|------|------|
| 1 | 커버 (오늘의 가격, 헤드라인) | dark |
| 2 | 시황 요약 (4개 핵심 지표) | bright |
| 3 | 포인트 1 / 4 | bright |
| 4 | 포인트 2 / 4 | bright |
| 5 | 포인트 3 / 4 | bright |
| 6 | 포인트 4 / 4 | bright |
| 7 | CTA(팔로우) + 면책 안내 (하단 작은 글씨) | dark |

- 페이지 인디케이터(dots)는 **7개**로 표시, active 위치만 카드별로 변경
- 모든 카드 하단 중앙에 "이진트" 로고 표기 (영문 부제 없음)

### 카드 7 CTA + 면책 가이드 (3단 구성)
- **메인 — 정중앙 3단 구성** (`.cta__main` flex column, gap 50px)
  - 헤드라인 (`.cta__headline`, 58px / 900): "복잡한 시장, **핵심만 빠르게!**"
    - "핵심만 빠르게!"만 오렌지 강조
  - 메시지 (`.cta__message`, 42px / 700): "매일의 경제 흐름을 / **이진트 카드뉴스**로 받아보세요."
    - "이진트 카드뉴스"만 오렌지 강조
  - 팔로우 안내 (`.cta__follow`, 36px / 600, 크림색): "팔로우하면 업로드 즉시 / 실시간으로 확인할 수 있어요."
    - 어둡거나 작으면 가독성이 떨어지므로 크림 본색 + 36px 이상 유지
  - 라벨, 팔로우 버튼, 핸들 등 부속 UI 없음 — 메시지에 집중
- **하단 — 면책 영역** (`.cta__disclaimer`)
  - 글자 크기 **28px** (브랜드 "이진트" 로고와 동일 크기) — 가독성 확보
  - 색상 `rgba(245, 233, 216, 0.45)` — 메인 메시지보다 약하지만 읽힘
  - 문구: "본 콘텐츠는 학습·정보 공유 목적입니다. 투자 권유가 아니며, / 모든 투자 결정과 결과의 책임은 본인에게 있습니다."
  - 브랜드 로고 아래에 배치

---

## 4. 빌드 / 변환

- HTML 작성 → `cards/card1.html` ~ `card7.html`
- 공용 스타일 → `cards/style.css`
- PNG 변환 → `python convert.py` (Playwright + Chromium, 1080×1350, 2x scale)
- 결과물 → `png/card1.png` ~ `card7.png`
- 변환 스크립트는 `card*.html` 패턴으로 자동 인식하므로 카드 추가 시 별도 수정 불필요

---

## 5. 부속 산출물

- `블로그_2026-05-06.txt` — 블로그용 장문 텍스트
  - 카드 톤과 동일하게 리딩성 표현 제거, 마지막에 면책 안내 포함
  - 새 시황을 만들 때 날짜를 바꿔 동일 형식으로 작성

- `인스타_더보기_2026-05-06.txt` — 인스타그램 게시물 캡션(더보기)
  - 첫 줄에 헤드라인 (스크롤 멈춤 효과)
  - 핵심 지표 요약 → 4개 포인트 bullet 정리 → CTA → 면책 → 해시태그 순
  - bullet은 `·` 사용 (이모지·기호 금지 규칙 유지)
  - 해시태그: 비트코인/BTC/ETF/암호화폐/경제 관련 + 브랜드 #이진트
